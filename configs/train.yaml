defaults:
  - backend: longcat
  - datamodule: qwen_cross_text
  - model: default
  - model/route: fm
  - loss: default
  - pl_module: default
  - runtime: default
  - callback: default
  - trainer: default
  - _self_
  - experiment: null

seed: 0
output_dir: null
output_subdir: ${backend.name}/${model.route}-${model.backbone.layers}l/${model.backbone.embedding_initialization}
