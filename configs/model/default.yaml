feature_adapter: none
feature_codebooks: 1
backbone:
  hidden_dim: 1024
  layers: 4
  heads: 8
  ffn_ratio: 4
  embedding_initialization: codec
  seed: 0
head:
  codebook_initialization: codec
  seed: 0
  hidden_dim: null
  layers: 8
  heads: 8
  ffn_ratio: 4
  fm_mode: flow
  anchor_context: local
  anchor_target: feature
  factor_predictor: parallel
  anchor_hidden_dim: 512
  anchor_layers: 4
  anchor_kernel_size: 3
  anchor_cosine_weight: 0.1
  anchor_factor_weight: 0.1
  anchor_factor_temperature: 0.1
